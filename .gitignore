# Reverse-engineering artifacts — regeneratable, large, and not what we ship.
# Reference only; keep them locally if you need them.
xapk_extracted/
jadx_output/
rootAVD/
*.xapk
*.apk

# Rust
target/
**/*.rs.bk

# Node / Tauri / Vite
node_modules/
dist/
.svelte-kit/
build/

# Secrets — never commit. The deviceSecret lives in your shell env / password manager.
.env
.env.local
secrets/
*.secret

# Debug/recon scripts — ad-hoc local tooling, not part of the shipped app.
.tmp/

# Personal sandbox config (ai-jail, etc.) — machine-local.
.ai-jail

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
